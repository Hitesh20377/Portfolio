# Hitesh Kandpal Advanced Portfolio

Open `index.html` with Live Server.

Important:
- Keep `index.html`, `style.css`, `script.js` in the root folder.
- Keep resume at `assets/resume.pdf`.
- Replace placeholder resume with your real resume.
- Edit links in `index.html` for exact GitHub repositories and research PDFs.

This package is generated as a large 9500-line style advanced portfolio starter.
